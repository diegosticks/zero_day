This project is an optional project
This is to learn the bsaic git commands
Close from git using git clone https://{acces token}@github.com/{username}/{project name}.git
Create your read me file, then add to git
